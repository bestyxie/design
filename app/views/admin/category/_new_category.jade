.modal.fade#new
  .modal-wrapper.modal-dialog
    .modal-header
      h3 新建分类
      a.close(href="javascrip:;",data-dismiss="modal")
        i.fa.fa-close
    .modal-content
      form#newcategory(action="/admin/category/new",method="POST")
        .form-group
          label(for="name") 标签名称*
          .input-box
            input#name.required(type="text",name="category[name]")
        .form-group
          label(for="price") 描述*
          .input-box
            input#price.required.rqrednum(type="text",name="category[desc]")
    .modal-footer
      button.next.disabled(type="submit") 确定
      button.cancle 取消